## Requestor Tracker: Third column for request details

PRD: "Requestor Tracker: Add third column showing request details (same UI as Request Debugger) when View button is clicked"

Changes:
- Added state vars for tracker detail view (trackerViewRequestId, trackerViewLoading, trackerViewError, trackerViewData)
- Added viewRequestInTracker() fn to fetch request data for inline display
- Added closeTrackerView() fn to dismiss detail panel
- Modified "View" btn to call viewRequestInTracker instead of switching tabs
- Added third column with request card UI (reuses same layout as Request Debugger)
- Grid switches from 2-col to 3-col layout when detail panel open
- Active view button highlighted in blue

Files: web/src/App.svelte

Build/tests pass.

---

## Requestor Tracker: UI improvements

PRD: "Requestor Tracker improvements: Remove nickname field from request order ID adder, add "Orders" heading under current requestor, remove OK button, replace "note" text with note icon"

Changes:
- Removed nickname input field from add-request form (requests use truncated ID as nickname)
- Removed newRequestNickname state var
- Added "Orders" heading above request list
- Removed "ok" text from toggle button - now just shows "!" always, faded when not problematic
- Replaced "note" text with document SVG icon
- Note button highlights blue when request has a note
- Shortened "Add Request" button text to "Add"

Files: web/src/App.svelte

Build/tests pass.

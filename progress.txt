## 2026-01-15: Requestor Tracker Tab

Task: Add "Requestor Tracker" tab to web app (PRD Planned item)

### Decisions
- Used localStorage for persistence (simple, no backend needed)
- Split-pane UI: sidebar for requestors, main area for requests
- "View in debugger" button integrates with existing Request Debugger tab
- Problematic requests highlighted with red border

### Files Changed
- web/src/App.svelte: Added tracker tab, types, state, UI, and styles

### Features Implemented
- Add/remove requestors with address + nickname
- Add/remove requests per requestor with ID + nickname
- Mark requests as problematic (toggleable)
- Add/edit notes on requests
- Quick "view" button to jump to Request Debugger
- All data persists in localStorage

### Notes
- Fixed a11y warnings by using button instead of clickable li
- Build passes, all tests pass

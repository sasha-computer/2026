## 2026-01-15: Explorer link button for order rows

PRD item: "add a button to each request next to copy that takes you to the request ID"

Changes:
- Added external link button next to copy button in order rows
- Links to explorer.boundless.network/orders/{requestId}?from=requestors/{requestorAddress}
- Uses external-link SVG icon, matches copy button styling
- stopPropagation prevents row click when clicking link

Files: web/src/App.svelte

Build + tests pass.

---

## 2026-01-15: Auto-fetch recent orders on requestor add

PRD: "when you add a new requestor, the 10 most recent orders are added and populated"

Changes:
- Changed addRequestor() to async, calls autoPopulateOrders(10) after selecting requestor
- Reuses existing autoPopulateOrders() logic (no duplication)

Files: web/src/App.svelte

---

## Normalize Supabase env var names

PRD: "Normalize Supabase env vars to `SUPABASE_URL`/`SUPABASE_API_KEY` for client config"

Changes:
- Added SUPABASE_* env prefix support in Vite config
- Switched Supabase client init to prefer SUPABASE_URL and SUPABASE_API_KEY with VITE_* fallback

Files: web/vite.config.ts, web/src/lib/supabase.ts, PRD.md

Notes: No runtime verification without cloud env secrets.

## Order row UI improvements

PRD: "problematic button furthest left, close button nicer with icon, copy button next to short ID"

---

## 2026-01-15: Fetch button + badge positioning fix

PRD item: "Not a big fan of the fetch recent orders button being disabled all the time..." + badge positioning

Changes:
- Fetch Recent Orders btn no longer disabled during `checkingNewOrders`, only during `autoPopulateLoading`
- Shows "..." badge with pulse animation while checking for new orders
- Requestor badge repositioned to top-right corner (absolute pos, -6px offset)
- Badge has hover scale effect, shadow for depth

Files: web/src/App.svelte

Reasoning: Button was disabled when checking for new orders, preventing user from clicking even when badge showed new orders available. Separated loading states so user can still click button during background check.

---

## 2026-01-15: Supabase module creation

PRD item: "Create `web/src/lib/supabase.ts` module with: Supabase client init, TrackerData/TrackedRequestor/TrackedRequest types, async loadTrackerData() (Supabase-first with localStorage fallback), async saveTrackerDataAsync() (saves to both), migrateLocalStorageToSupabase() for one-time migration"

Changes:
- Created `web/src/lib/supabase.ts` with Supabase + localStorage persistence helpers
- Exports TrackedRequest, TrackedRequestor, TrackerData types
- Supabase client singleton via getSupabaseClient()
- isSupabaseConfigured() helper for UI conditional logic
- loadTrackerData(): async, Supabase-first with localStorage fallback and timestamp conflict resolution
- saveTrackerDataAsync(): async, saves to localStorage + Supabase with updatedAt
- migrateLocalStorageToSupabase(): one-time migration helper with tracker ID and table checks
- Uses SUPABASE_URL/SUPABASE_API_KEY with VITE_* fallback

Files: web/src/lib/supabase.ts (new)

Notes: Table schema assumed: requestor_tracker(id text primary key, data jsonb).

---

## 2026-01-15: App.svelte Supabase integration

PRD item: "Update App.svelte for Supabase: import from ./lib/supabase, remove inline type defs, add trackerLoading state, add $effect for async init with migration, delete old load/save functions, replace saveTrackerData() calls with saveTrackerDataAsync(trackerData), add loading UI to tracker tab"

Changes:
- Imported types and functions from `./lib/supabase`
- Removed inline type definitions
- Added trackerLoading state, initialized trackerData as empty
- Added $effect for async init (load + migrate)
- Deleted old load/save functions
- Replaced saveTrackerData() calls with saveTrackerDataAsync(trackerData)
- Added loading UI (spinner + message) to tracker tab when trackerLoading is true
- Added CSS for `.tracker-loading` and `.loading-spinner`

Files: web/src/App.svelte

Notes: Migration runs only when Supabase env vars are configured. Falls back to localStorage-only if not. Loading state prevents flickering/race conditions during async init.

---

## 2026-01-15: Supabase integration tests

PRD item: "Test Supabase integration: add requestor (verify in Supabase), add orders, reload page (data persists), test offline fallback, test localStorage migration"

Changes:
- Created `web/src/lib/supabase.test.ts` with 14 unit tests covering:
  - isSupabaseConfigured(): returns false when env vars missing
  - loadTrackerData(): localStorage fallback, malformed JSON handling, Supabase load, error fallback
  - saveTrackerDataAsync(): localStorage-only save, dual save, graceful Supabase failure
  - migrateLocalStorageToSupabase(): skip when unconfigured, skip when empty, skip when remote has data, successful migration
- All tests pass (21 total including existing client tests)

Files: web/src/lib/supabase.test.ts (new)

Notes: Tests mock Supabase client and localStorage. Covers all code paths including error handling. Manual testing still recommended for full E2E verification with real Supabase instance.

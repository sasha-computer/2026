## Auto-fetch orders on new requestor add

PRD: "when you add a new requestor, the 10 most recent orders are added and populated"

Changed addRequestor() to async, calls autoPopulateOrders(10) after setting selectedRequestorIndex.
Reuses existing autoPopulateOrders() logic - no code duplication.

Files: web/src/App.svelte (lines 101-117)

Build + tests pass.

## Normalize Supabase env var names

PRD: "Normalize Supabase env vars to `SUPABASE_URL`/`SUPABASE_API_KEY` for client config"

Changes:
- Added SUPABASE_* env prefix support in Vite config
- Switched Supabase client init to prefer SUPABASE_URL and SUPABASE_API_KEY with VITE_* fallback

Files: web/vite.config.ts, web/src/lib/supabase.ts, PRD.md

Notes: No runtime verification without cloud env secrets.
## Order row UI improvements

PRD: "problematic button furthest left, close button nicer with icon, copy button next to short ID"

Changes:
- Added `.request-actions-left` div containing problematic (!) button at leftmost position
- Moved copy button inline next to short order ID in header line
- Replaced "x" text in remove button with X icon (SVG)
- Changed `.request-row` from `<button>` to `<div>` with role="button" to allow nested copy button
- Added focus state for keyboard accessibility

Files: web/src/App.svelte (lines 993-1047, CSS ~2086-2161)

Build + tests pass.

## Per-requestor notification badges

PRD: "add notification badge from Fetch Recent Orders to Tracked Requestors column on each requestor. Clicking badge shows spinner, fetches orders, opens requestor view with most recent order."

Changes:
- Added `requestorNewOrderCounts` state (Record<number, number>) to track per-requestor counts
- Added `loadingRequestorBadge` state to track which requestor badge is loading
- Added `checkForNewOrdersForRequestor(index)` to check single requestor
- Added `checkAllRequestorsForNewOrders()` to check all on load via $effect
- Added `handleRequestorBadgeClick()` to fetch orders, show spinner, select requestor, open most recent order in view
- Badge UI in requestor list sidebar with red badge showing count
- Spinner animation on badge while loading (orange background)
- After fetch: selects requestor, clears badge, opens most recent new order in right panel

Files: web/src/App.svelte (lines 82-84, 394-487, 908-922, CSS 1790-1831)

Build + tests pass.

## Supabase persistence for Requestor Tracker

PRD: "Create web/src/lib/supabase.ts module... update App.svelte for Supabase... test Supabase integration"

Changes:
- Added supabase client helper + TrackerData types + load/save/migrate helpers with localStorage fallback
- Swapped Requestor Tracker persistence to async Supabase-backed save/load with migration on init
- Added tracker loading UI to avoid interactions before data loads

Decisions:
- Use Supabase table `requestor_tracker` keyed by a local tracker ID; saves always update localStorage first for offline fallback

Files: web/src/lib/supabase.ts, web/src/App.svelte, PRD.md

Notes: Supabase test steps pending (manual verification in UI + Supabase table).
